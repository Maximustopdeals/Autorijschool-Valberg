import { reviews } from './content';

export { reviews };

// Samenvatting van de Google-beoordeling (wordt ook in de schema-data gebruikt)
export const site_rating = {
  score: '5,0',
  count: 10,
} as const;
